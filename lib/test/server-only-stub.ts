// Test stub for the `server-only` package. The real package throws when
// imported into a client bundle; in Node/Vitest there's nothing to guard, so
// this is intentionally empty. Wired in via vitest.config.ts `resolve.alias`.
export {};
